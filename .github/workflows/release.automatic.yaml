name: Release New Version

on:
  pull_request:
    types:
      - closed

permissions:
  contents: write

jobs:
  release:
    uses: openkcm/build/.github/workflows/release.base.yaml@main
    with:
      release-type: 'node'
    secrets: inherit
