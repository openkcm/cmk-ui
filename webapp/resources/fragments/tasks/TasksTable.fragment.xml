<core:FragmentDefinition xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:tnt="sap.tnt"
    core:require="{ formatMessage: 'sap/base/strings/formatMessage', commonFormatter: 'kms/common/Formatters' }"
>
    <Table
        id="tasksTable"
        inset="false"
        items="{oneWay>/workflowTasks}">
        <headerToolbar>
            <OverflowToolbar>
                <Title
                    text="{parts: ['i18n>workflowTasksCount', 'oneWay>/workflowTasksCount'], formatter: 'formatMessage'}"
                    level="H2" />
                <ToolbarSpacer />
                <Button
                    icon="sap-icon://sort"
                    press="onTaskTableSortPress"
                    class="sapUiSmallMarginEnd"
                    type="Transparent" />
            </OverflowToolbar>
        </headerToolbar>
        <columns>
            <Column minScreenWidth="Tablet" demandPopin="true">
                <Text text="{i18n>name}" />
            </Column>
            <Column minScreenWidth="Tablet" demandPopin="true">
                <Text text="{i18n>taskId}" />
            </Column>
            <Column minScreenWidth="Tablet" demandPopin="true">
                <Text text="{i18n>createdOn}" />
            </Column>
            <Column minScreenWidth="Tablet" demandPopin="true">
                <Text text="{i18n>status}" />
            </Column>
        </columns>
        <items>
            <ColumnListItem vAlign="Middle" type="Navigation" press="onTaskPress">
                <cells>
                    <ObjectIdentifier
                        title="{parts: ['oneWay>artifactType'], formatter: '.getText'}"
                        text="{parts: ['oneWay>actionType'], formatter: '.getText'}"
                    />
                    <Text text="{oneWay>id}" />
                    <Text
                        text="{path:'oneWay>metadata/createdAt', type:'sap.ui.model.type.DateTime', formatOptions:{UTC: true, source:{pattern: 'yyyy-MM-ddTHH:mm:ssXXX'}}} UTC" />
                    <ObjectStatus
                        icon="{parts: ['oneWay>state'], formatter: 'commonFormatter.setTaskStatus'}"
                        text="{parts: ['oneWay>state'], formatter: '.getText'}"
                        active="true"
                        inverted="true"
                        state="{parts: ['oneWay>state'], formatter: 'commonFormatter.setTaskStatusIndicationState'}" />
                </cells>
            </ColumnListItem>
        </items>
    </Table>
</core:FragmentDefinition>