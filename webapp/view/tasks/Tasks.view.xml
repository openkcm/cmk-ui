<mvc:View
    controllerName="kms.controller.tasks.Tasks"
    xmlns="sap.m"
    xmlns:f="sap.f"
    xmlns:mvc="sap.ui.core.mvc">
    <f:DynamicPage>
        <f:title>
            <f:DynamicPageTitle>
                <f:breadcrumbs>
                    <Breadcrumbs currentLocationText="{i18n>sideContentTasks}">
                        <Link text="{i18n>sideContentHome}"
                            press=".onBreadCrumbLinkPress($event, 'home')" />
                    </Breadcrumbs>
                </f:breadcrumbs>
                <f:heading>
                    <Title text="{i18n>sideContentTasks}" />
                </f:heading>
                <f:snappedTitleOnMobile>
                    <Title text="{i18n>sideContentTasks}" />
                </f:snappedTitleOnMobile>
            </f:DynamicPageTitle>
        </f:title>
        <f:content>
        </f:content>
    </f:DynamicPage>
</mvc:View>